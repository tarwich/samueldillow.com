---
import { RESERVATIONS } from '../../_data';

export function getStaticPaths() {
  return RESERVATIONS.map((reservation) => ({
    params: { id: reservation.id },
  }));
}

return Astro.redirect(
  `/portfolio/airpeggio/reservations/${Astro.params.id}/checklist/`,
);
---
